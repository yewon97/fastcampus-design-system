{
  "name": "@fastcampus/themes",
  "version": "0.0.1",
  "scripts": {
    "build": "esbuild src/index.js --minify --format=cjs --bundle --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.16.7"
  }
}
