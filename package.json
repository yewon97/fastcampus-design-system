{
  "name": "fastcampus-design-system",
  "version": "0.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.9.2",
  "devDependencies": {
    "nx": "^21.2.1",
    "typescript": "^5.2.2"
  }
}
